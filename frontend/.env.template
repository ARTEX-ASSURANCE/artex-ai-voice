# Environment variables for Artex Agent Frontend (Vite)
# Copy this file to .env in this directory (artex_agent/frontend/) and customize.

# Base URL for the backend API.
# When running both frontend and backend via docker-compose, the frontend (in browser)
# needs to access the backend via the port exposed on the HOST machine by the backend service.
# In docker-compose.yml, the backend service's port 8000 is mapped to host port 8001.
VITE_API_BASE_URL=http://localhost:8001
