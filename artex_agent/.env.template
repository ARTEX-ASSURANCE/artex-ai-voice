# Artex Agent Environment Variables Template
# Copy this file to .env and fill in your actual values.
# Do NOT commit the .env file to version control.

# Gemini API Configuration
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# LiveKit Server Configuration
LIVEKIT_URL=wss://your-livekit-server-url.livekit.cloud
LIVEKIT_API_KEY=YOUR_LIVEKIT_API_KEY_HERE
LIVEKIT_API_SECRET=YOUR_LIVEKIT_API_SECRET_HERE
# LIVEKIT_WEBHOOK_SECRET=your_webhook_shared_secret_for_verifying_incoming_livekit_webhooks

# MySQL Database Configuration
# Example: DATABASE_URL=mysql+aiomysql://user:password@host:port/database_name
DATABASE_URL=YOUR_DATABASE_URL_HERE

# API Configuration (e.g., for FastAPI)
# Comma-separated list of allowed origins for CORS.
# Example: ALLOWED_ORIGINS="http://localhost:3000,http://127.0.0.1:3000,https://your-frontend-domain.com"
# ALLOWED_ORIGINS="http://localhost:3000"

# Google Cloud Text-to-Speech (Optional, for higher quality TTS)
# If you want to use Google Cloud TTS, uncomment and set the path to your service account key JSON file.
# Ensure the service account has permissions for Text-to-Speech API.
# GOOGLE_APPLICATION_CREDENTIALS=path/to/your/google_cloud_service_account_key.json
TTS_USE_GOOGLE_CLOUD=true # Set to false to force gTTS even if GOOGLE_APPLICATION_CREDENTIALS is set
TTS_LANG_CODE=fr-FR # For Google Cloud TTS, e.g., fr-FR
TTS_VOICE_NAME=fr-FR-Standard-D # For Google Cloud TTS, e.g., fr-FR-Standard-D (WaveNet D)
TTS_CACHE_DIR=/tmp/artts_cache # Directory for cached TTS audio files

# Localization and Timezone
# These are often set at the OS/container level but can be useful for scripts/apps
LANG=fr_FR.UTF-8
TZ=UTC

# Sentry DSN (for error tracking - if/when Sentry is integrated)
# SENTRY_DSN=YOUR_SENTRY_DSN_HERE
